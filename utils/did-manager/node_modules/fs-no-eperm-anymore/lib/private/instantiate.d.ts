import { FUNCTIONS } from "./constants";
import { AttemptOptions, Options } from "./model";
export declare function instantiate(options?: Options): typeof FUNCTIONS;
export declare function resolveAttemptOptions(fnName: keyof typeof FUNCTIONS, errorCode: string, { items }: Options): AttemptOptions | undefined;

export declare type TODO = any;
export declare type Arguments<F extends (...x: TODO[]) => TODO> = F extends (...x: infer A) => TODO ? A : never;
export declare type Omit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;
